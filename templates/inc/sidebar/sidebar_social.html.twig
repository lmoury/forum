<div class="sidebar-content">
    <div class="sidebar-title">
        <span class="fa fa-share-alt"></span>
        <span class="">Restez connecté</span>
    </div>
    <div class="sidebar-body text-center">
        <div class="icon-social-container egeon">
            <div class="icon-social-content">
                {% for social in sociaux if social.actif == true %}
                <a href="{{ social.url }}" target="_blank" class="{{ social.class }}" title="Join us on {{ social.nom }}"><i class="fa {{ social.icon }}" aria-hidden="true"></i></a>
                {% endfor %}
            </div>
        </div>
    </div>
</div>
