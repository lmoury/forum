{% extends 'layout.html.twig' %}

{% block title %} Home {{ parent() }}{% endblock %}

{% block body %}

<div class="container">
    <ol class="breadcrumb">
        <li class="breadcrumb-item active">Home</li>
	</ol>

    <div class="row">
        <div class="col-sm-12">
            home
            {% if is_granted('ROLE_BANNI') %}
            <div class="alert alert-danger">
                <strong>Vous êtes banni du site.</strong>
            </div>
            {% endif %}
        </div>
    </div>
</div>
{% endblock %}
