<div class="modal-header">
    <h4 class="modal-title" id="exampleModalTitle">
        Ajouter un membre de l'équipe dans cette conversation
    </h4>
    <button type="button" class="close" data-dismiss="modal" aria-label="Close">
        <span aria-hidden="true">&times;</span>
    </button>
</div>
<div class="modal-body-alert">
    <div class="alert alert-warning">
        <strong>Note:</strong> Veuillez sélectionner le membre de l'équipe que vous souhaitez ajouter dans ce message privé.
    </div>
</div>
<div class="modal-body">
    {{ form_start(form) }}
    <div class="">
       <dl class="formRow formRow-input">
           <dt>
               <label for="email">Membre de l'équipe : </label>
           </dt>
           <dd>
               <div class="form-group">
                   <div class="input-group mb-2">
                       {{ form_widget(form.participant) }}
                       {{ form_errors(form.participant) }}
                   </div>
               </div>
           </dd>
       </dl>
    </div>
    <div class="modal-footer">
        {{ form_rest(form) }}
        <div class="text-center btn-container">
            <button class="btn"><i class="fa fa-save"></i> {{ button|default('Sauvegarder')}}</button>
        </div>
    </div>
    {{ form_end(form) }}
</div>
