<div class="modal-header">
    <h4 class="modal-title" id="exampleModalTitle">
        Bannir le membre {{ membre.username }}
    </h4>
    <button type="button" class="close" data-dismiss="modal" aria-label="Close">
        <span aria-hidden="true">&times;</span>
    </button>
</div>
<div class="modal-body-alert">
    <div class="alert alert-warning">
        <strong>Note:</strong> Veuillez sélectionner la raisons du bannissement et optionnelement son temps de bannissement.
    </div>
</div>
<div class="modal-body">
    {{ form_start(form) }}
    <div class="">
       <dl class="formRow formRow-input">
           <dt>
               <label for="email">Raisons du bannissement : </label>
               <dfn class="formRow-hint">Requis</dfn>
           </dt>
           <dd>
               <div class="form-group">
                   <div class="input-group mb-2">
                       {{ form_widget(form.motif) }}
                       {{ form_errors(form.motif) }}
                   </div>
               </div>
           </dd>
       </dl>
       <dl class="formRow formRow-input">
           <dt>
               <label for="email">Temps du bannissement : </label>
           </dt>
           <dd>
               <div class="form-group">
                   <div class="input-group mb-2">
                       {{ form_widget(form.fin) }}
                       {{ form_errors(form.fin) }}
                   </div>
               </div>
           </dd>
       </dl>
    </div>
    <div class="modal-footer">
        {{ form_rest(form) }}
        <div class="text-center btn-container">
            <button class="btn"><i class="fa fa-save"></i> {{ button|default('Bannir')}}</button>
        </div>
    </div>
    {{ form_end(form) }}
</div>
